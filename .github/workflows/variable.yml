name: variables
on: 
    push: 
        branches: 
            main
jobs:
    var1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo 'env = ${{ vars.user }}'